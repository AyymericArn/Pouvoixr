// uniform sampler tAudioData;
// varying vec3 vUv;

// uniform float uWaveForm;

// varying vec3 vNormal;
// varying float wWaveEffect;

// void main () {

//     vec4 modelPosition = modelMatrix * vec4(position, 1.0);

//     vec4 newPosition = (position.x)

//     float waveEffect = sin(modelPosition.y * 30 + uTime * 0.03)
// }